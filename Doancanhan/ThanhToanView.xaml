<Window x:Class="Doancanhan.ThanhToanView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thanh Toán Hóa Đơn" Height="650" Width="700"
        WindowStartupLocation="CenterScreen" Background="#F4F6F9"
        Loaded="Window_Loaded">
    <Window.Resources>
        <!-- Style cho các nút bấm -->
        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Background" Value="#2ECC71"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#27AE60"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" x:Key="SecondaryButtonStyle" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#95A5A6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#7F8C8D"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock x:Name="lblMaDonHang" Grid.Row="0" FontWeight="Bold" FontSize="24" 
                   Text="HÓA ĐƠN #" Foreground="#2C3E50" Margin="0,0,0,15"/>

        <!-- Bảng chi tiết món ăn -->
        <DataGrid x:Name="dgChiTietDonHang" Grid.Row="1" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column"
                  AlternatingRowBackground="#EAF2F8" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên Món Ăn" Binding="{Binding MonAn.TenMonAn}" Width="*"/>
                <DataGridTextColumn Header="Số Lượng" Binding="{Binding SoLuong}" Width="100"/>
                <DataGridTextColumn Header="Đơn Giá" Binding="{Binding MonAn.Gia, StringFormat='N0', ConverterCulture='vi-VN'}" Width="120"/>
                <DataGridTextColumn Header="Thành Tiền" Binding="{Binding ThanhTien, StringFormat='N0', ConverterCulture='vi-VN'}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Khu vực thông tin thanh toán -->
        <Border Grid.Row="2" Margin="0,15,0,0" Padding="15" Background="White" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Labels -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Tổng tiền:" FontWeight="SemiBold" Margin="5" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Giảm giá:" FontWeight="SemiBold" Margin="5" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Khách cần trả:" FontWeight="Bold" Foreground="#E74C3C" Margin="5" VerticalAlignment="Center" FontSize="16"/>
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Khách đưa:" FontWeight="SemiBold" Margin="5" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Tiền thối:" FontWeight="Bold" Margin="5" VerticalAlignment="Center" FontSize="16"/>

                <!-- Values -->
                <TextBlock x:Name="lblTongTien" Grid.Row="0" Grid.Column="1" Text="0 VNĐ" Margin="5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <TextBlock x:Name="lblTienGiamGia" Grid.Row="1" Grid.Column="1" Text="0 VNĐ" Margin="5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <TextBlock x:Name="lblTongTienTrungGian" Grid.Row="2" Grid.Column="1" Text="0 VNĐ" FontWeight="Bold" Foreground="#E74C3C" Margin="5" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="16"/>
                <TextBox x:Name="txtKhachDua" Grid.Row="3" Grid.Column="1" Margin="5" Padding="5" TextChanged="TxtKhachDua_TextChanged" TextAlignment="Right"/>
                <TextBlock x:Name="lblTienThoi" Grid.Row="4" Grid.Column="1" Text="0 VNĐ" FontWeight="Bold" Margin="5" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="16"/>

                <!-- Buttons -->
                <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button x:Name="btnXacNhanThanhToan" Content="XÁC NHẬN THANH TOÁN" Style="{StaticResource PrimaryButtonStyle}" Width="250" Click="BtnXacNhanThanhToan_Click"/>
                    <Button x:Name="btnInHoaDon" Content="In Hóa Đơn" Style="{StaticResource SecondaryButtonStyle}" Width="150" Margin="10,0,0,0" Click="BtnInHoaDon_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
